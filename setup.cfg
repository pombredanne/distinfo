[aliases]
test = pytest

[wheel]
universal=1

[tool:pytest]
addopts =
    --color=yes
    --strict
    --showlocals
    --tb=short
    --cov
    --cov-report term-missing
    --cov-report html:.htmlcov
    --cov-report xml
    --no-cov-on-fail
    --ignore=result
